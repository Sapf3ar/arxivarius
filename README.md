## Testing

To run the tests, use the following command:

```bash
python -m unittest discover -s tests
```

Ensure you have all dependencies installed and follow any additional instructions for setting up the testing environment.